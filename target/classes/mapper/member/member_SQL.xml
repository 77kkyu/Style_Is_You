<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="join">
	
	<!-- 회원추가 -->
	<insert id="insertMember" parameterType="hashmap">
		<![CDATA[
			INSERT INTO MEMBER
			(
				MEMBER_NO,
			    MEMBER_ID, 
			    MEMBER_PASSWD, 
			    MEMBER_NAME, 
			    MEMBER_BIRTH, 
			    MEMBER_EMAIL, 
			    MEMBER_PHONE,
			    MEMBER_ZIPCODE,
			    MEMBER_ADDR1,
			    MEMBER_ADDR2,
			    MEMBER_DATE,
			    MEMBER_GRADE,
			    MEMBER_LOG
			)
			VALUES
			(
				MEMBER_NO_SEQ.NEXTVAL,
				#{MEMBER_ID},
				#{MEMBER_PASSWD},
				#{MEMBER_NAME},
				#{MEMBER_BIRTH},
				#{MEMBER_EMAIL},
				#{MEMBER_PHONE},
				#{MEMBER_ZIPCODE},
				#{MEMBER_ADDR1},
				#{MEMBER_ADDR2},
				sysdate,
				'NORMAL',
				sysdate
			)
		]]>
	</insert>
	
	<!-- 회원가입시 포인트 테이블에 '0' 추가 -->
	<insert id="insertDefaultPoint" parameterType="hashmap">
		<selectKey keyProperty="MEMBER_NO" resultType="int" order="BEFORE">
			SELECT 
				MAX(MEMBER_NO)
			FROM 
				MEMBER
		</selectKey>
		<![CDATA[
			INSERT INTO POINT
			(
				POINT_NO,
			    MEMBER_NO, 
			    POINT_VAL,
			    POINT_TOTAL
			)
			VALUES
			(
				POINT_NO_SEQ.NEXTVAL,
				#{MEMBER_NO},
				0,
				0
			)
		]]>
	</insert>
	
	<!-- 아이디 중복확인-->
	<select id="selectIdCheck" parameterType="String" resultType="int">
	<![CDATA[
		SELECT COUNT(*) FROM MEMBER WHERE MEMBER_ID = #{MEMBER_ID}
	]]>
	</select>
	
	<!-- 주문 후 회원정보 수정 -->
	<update id="updateMemberTotal" parameterType="hashmap">
	<selectKey keyProperty="PRICE" resultType="int" order="BEFORE">
			SELECT 
				MEMBER_TOTAL+ ${ORDER_TOTAL_ORDER_PRICE}
			FROM 
				MEMBER
			WHERE 
				MEMBER_NO = #{MEMBER_NO}
		</selectKey>
		<![CDATA[
			UPDATE MEMBER 
				SET
    				MEMBER_TOTAL = ${PRICE}
			WHERE
				MEMBER_NO = ${MEMBER_NO}
		]]>
	</update>
	<!-- 
	<update id="updateMemberGrade" parameterType="hashmap">
			UPDATE MEMBER 
				SET
    		<choose>
                <when test="MEMBER_TOTAL lt 200000">
                    MEMBER_GRADE = 'NORMAL'
                </when>
                <when test="MEMBER_TOTAL lt 500000 and MEMBER_TOTAL gte 200000">
                    MEMBER_GRADE = 'GOLD'
                </when>
                <otherwise>
                    MEMBER_GRADE = 'VIP'
                </otherwise>
            </choose>
			WHERE
				MEMBER_NO = ${MEMBER_NO}
	</update>
		 -->
</mapper>