<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Qna">

	<select id="selectQnaList" parameterType="hashmap" resultType="hashmap">/>	
		<![CDATA[
			SELECT * FROM QNA
		]]>
	</select>
	
	<insert id="insertQna" parameterType="hashmap"
	useGeneratedKeys="true" keyProperty="QNA_NO">
no, 제목 글쓴이 등록일
		<![CDATA[
			INSERT INTO QNA
			( QNA_NO, MEMBER_NO, QNA_NAME, QNA_LEVEL, QNA_CATEGORY, QNA_TITLE, QNA_CONTENT, QNA_DATE, QNA_SECRET, QNA_PASSWD )
			VALUES
			( QNA_NO_SEQ.nextval ,
			MEMBER_NO_SEQ.nextval ,
			#{QNA_NAME} ,
			#{QNA_LEVEL} ,
			#{QNA_CATEGORY} ,
			#{QNA_TITLE} ,
			#{QNA_CONTENT} ,
			SYSDATE ,
			#{QNA_SECRET} ,
			#{QNA_PASSWD} )
		]]>
	</insert>
	
	<select id="selectQnaDetail" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT 
				NOTICE_NO,
				NOTICE_TITLE,
				NOTICE_CONTENT,
				TO_CHAR(NOTICE_DATE, 'YYYY-MM-DD') AS NOTICE_DATE,
				MEMBER_NO
				FROM NOTICE
			WHERE
				NOTICE_NO = #{NOTICE_NO}
		]]>
	</select>
	
	
	<update id="updateQna" parameterType="hashmap">
		<![CDATA[
			UPDATE QNA
			SET
				QNA_TITLE = #{QNA_TITLE},
				QNA_CONTENT = #{QNA_CONTENT}
			WHERE
				QNA_NO = #{QNA_NO}
		]]>
	</update>
	
	
	<delete id="deleteQna" parameterType="hashmap">
		<![CDATA[
			DELETE FROM QNA
			WHERE 
				QNA_NO = #{QNA_NO}
		]]>
	</delete>

</mapper>